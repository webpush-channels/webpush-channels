language: python
python: 2.7
cache: pip
env:
  - TOX_ENV=py27
  - TOX_ENV=flake8
  - TOX_ENV=docs
install:
  - pip install tox
script: tox -e $TOX_ENV
after_success:
  - pip install coveralls
  - coveralls

matrix:
  include:
    - python: 3.6
      env:
        - TOX_ENV=py36
